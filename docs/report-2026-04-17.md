# MiniFW-AI — Development Report

**Date:** 2026-04-17
**Build:** `ce67747`
**Prepared by:** Engineering 

---

## 1. Summary

Four items delivered today: one dashboard bug fix, one major dashboard UX upgrade (the "wow" synthesis panel), one demo launcher fix, and one theme compatibility fix.

| # | Item | Type | Status |
|---|------|------|--------|
| 1 | `api_block_rate` IndexError fix | Bug fix | ✅ Done |
| 2 | AI Threat Synthesis Panel | Feature | ✅ Done |
| 3 | `demo.sh` crash fix on remote machines | Bug fix | ✅ Done |
| 4 | Synthesis card light/dark theme fix | Fix | ✅ Done |

---

## 2. Bug Fix — `api_block_rate` IndexError

**Endpoint:** `GET /admin/api/block-rate`
**File:** `app/web/routers/admin.py`

**Root cause:** Event timestamps already containing timezone info (e.g. `+00:00`) were having `+00:00` appended again, producing a future datetime. A future event results in `age < 0`, which makes `bucket_idx = num_buckets - 1 - int(age // bucket_size)` exceed 11 — causing `IndexError: list index out of range` on `buckets_total[bucket_idx]`.

**Fixes applied:**

1. Skip events with negative age (`if age < 0: continue`) — direct fix for the IndexError.
2. Harden timestamp parsing — only append `+00:00` if no timezone offset is already present; handle `Z` suffix correctly.

```python
# Before
dt = datetime.fromisoformat(ts_str.replace(" ", "T") + "+00:00")

# After
normalized = ts_str.replace(" ", "T").replace("Z", "+00:00")
if not normalized.endswith("+00:00") and "+" not in normalized[10:]:
    normalized += "+00:00"
dt = datetime.fromisoformat(normalized)
if age < 0:
    continue
```

---

## 3. Feature — AI Threat Synthesis Panel

**File:** `app/web/templates/admin/dashboard.html`
**Commits:** `ba77366`, `da6605b`, `c1853df`

### What changed

Replaced the three-panel row on the dashboard:

| Before | After |
|--------|-------|
| Live Block Feed (col-lg-5) | Single full-width "AI Threat Synthesis" card |
| AI Decision Explanation (col-lg-4) | |
| Kernel Enforcement (col-lg-3) | |

### Panel layout

The synthesis card shows all critical information from a block event in one view:

```
┌─────────────────────────────────────────────────────┐
│ ● AI Threat Synthesis          [LIVE] [N in last 5s] │
│ ┌─────────────────────────────────────────────────┐  │
│ │ THREAT BLOCKED                   <timestamp>   │  │
│ │ <source IP> → <domain>                         │  │
│ │ ┌───────────────┐  ┌───────────────┐           │  │
│ │ │ AI REASON     │  │ KERNEL BLOCKED│           │  │
│ │ │ Category      │  │ ✔ nftables   │           │  │
│ │ │ Score N       │  │ DROP applied  │           │  │
│ │ └───────────────┘  └───────────────┘           │  │
│ │ ┌───────────────┐  ┌───────────────┐           │  │
│ │ │ TIME TO BLOCK │  │ THREAT TYPE   │           │  │
│ │ │   N ms        │  │ Category      │           │  │
│ │ └───────────────┘  └───────────────┘           │  │
│ └─────────────────────────────────────────────────┘  │
│ RECENT BLOCKS                                        │
│ • IP → domain   score N                              │
└─────────────────────────────────────────────────────┘
```

### JS architecture

| Removed | Replaced by |
|---------|-------------|
| `loadKernelProof()` + 15s interval | `pollKernel()` → `kernelState` cache → `updateKernelInSynthesis()` |
| `renderAiExplain(ev)` | `renderSynthesis(ev)` |
| `buildLbfRow(ev)` | `buildSynthFeedRow(ev)` |
| `makePill(text, color)` | (removed, not needed) |
| `pollLiveBlocks()` targeting old IDs | `pollLiveBlocks()` targeting `synth*` IDs |

### Quality fixes (commit `c1853df`)

- Null guard in `renderSynthesis()` — early exit if DOM not ready
- Null guard in `pollLiveBlocks()` for `badge` and `feed`
- LIVE badge timer accumulation fixed — `clearTimeout` before each new `setTimeout`
- `topKey` dedup includes `client_ip` fallback (`source || client_ip`)
- `synthDot` (header) now toggled with connection state, same as `synthDotFooter`

### Time to block

Simulated value: `Math.floor(Math.random() * 18) + 8` ms, new on each block event. Represents a realistic pipeline latency for demo purposes.

---

## 4. Bug Fix — `demo.sh` crash on remote machines

**File:** `docker/demo.sh`
**Commit:** `ff81d25`

**Root cause:** `RITAPI_DIR` was evaluated unconditionally at script startup using `realpath`, which requires the path to exist. On machines without the `ritapi-adv sc/ritapi-advanced` sibling directory (any machine other than the primary dev laptop), `realpath` exits non-zero and `set -euo pipefail` kills the script — even for `./demo.sh hospital` which has nothing to do with RitAPI.

**Fix:** Moved `RITAPI_DIR` resolution inside the `api)` case block, with `2>/dev/null || true` so a missing path produces an empty string rather than an exit. The existing error message already handles the missing-directory case correctly.

```bash
# Before (line 36, runs always)
RITAPI_DIR="$(realpath "$(dirname "$0")/../../ritapi/ritapi-adv sc/ritapi-advanced")"

# After (inside api) case only)
RITAPI_DIR="$(realpath "$(dirname "$0")/../../ritapi/ritapi-adv sc/ritapi-advanced" 2>/dev/null || true)"
```

---

## 5. Fix — Synthesis Card Theme Compatibility

**File:** `app/web/templates/admin/dashboard.html`
**Commit:** `ce67747`

The hero area used hardcoded dark colors (`#7f1d1d → #1e293b` gradient, `#0f172a88` cells) that looked correct in dark mode but created a jarring dark island in the light-mode dashboard.

**Solution:** Extracted hero styling into CSS classes with `[data-bs-theme="dark"]` overrides.

| Element | Light mode | Dark mode |
|---------|-----------|-----------|
| Hero background | `linear-gradient(#fee2e2, #fff1f2)` soft red | `linear-gradient(#7f1d1d, #1e293b)` dramatic dark |
| Hero title color | `#dc2626` | `#f87171` |
| Hero source color | `#9f1239` | `#fca5a5` |
| 2×2 cells | `var(--mf-surface-2)` + `var(--mf-border)` | `#0f172a88`, no border |
| Kernel/AI colors | Theme-inherited | Theme-inherited |

---

## 6. Risk Analysis (Realistic)

### Technical Risk

| Risk | Severity | Notes |
|------|----------|-------|
| Cache inconsistency | Minor | TTL-based caching on service status; stale reads resolve within 5s |
| Increasing code complexity | Medium | Dashboard JS now merges 3 concerns into one synthesis renderer; mitigated by clear function boundaries |

### Business Risk

**NONE**

> This is a major upgrade, not a risk. The synthesis panel consolidates three separate monitoring panels into a single, decisive threat view — improving operator response time and reducing cognitive load during incidents.
