# MiniFW-AI — Development Report
**Date:** 2026-05-28  
**Version:** 2.2.0  
**Session type:** Product completion — Tutorial and Installation Program for all 6 products

---

## Summary

Full-day session completing the TUTORIAL and INSTALLATION PROGRAM deliverables for all
6 products on the checklist: MINIFW-AI Healthcare, Financial, Schools, Legal, RITAPI
Advanced, and RITAPI V-Sentinel. Also recovered deleted financial demo files from git
history, wrote accurate GUIDE.md documents for each sector (verified against source
files), created a root-level `build_deb.sh` wrapper, and copied all 6 USB demo
packages to a physical USB drive.

All three repos pushed to GitHub. USB: 4.0 GB across 6 packages, 25 GB remaining.

---

## Checklist — Final State

| Product | STATIC | LIVE | TUTORIAL | INSTALLATION PROGRAM |
|---------|--------|------|----------|----------------------|
| MINIFW-AI Healthcare | ✅ | ✅ | ✅ GUIDE.md | ✅ install.sh + build_deb.sh |
| MINIFW-AI Financial | ✅ | ✅ | ✅ GUIDE.md | ✅ install.sh + build_deb.sh |
| MINIFW-AI Schools | ✅ | ✅ | ✅ GUIDE.md | ✅ install.sh + build_deb.sh |
| MINIFW-AI Legal | ✅ | ✅ | ✅ GUIDE.md | ✅ install.sh + build_deb.sh |
| RITAPI Advanced | ✅ | ✅ | ✅ GUIDE.md | ✅ bootstrap.sh + build_deb.sh |
| RITAPI V-Sentinel | ✅ | ✅ | ✅ GUIDE.md | ✅ install.sh + build_deb.sh |

---

## 1. MINIFW-AI Healthcare — Tutorial + Installation Program

**Files created/modified:**
- `dist/minifw-usb-hospital-standalone-v2.2.0/GUIDE.md` — 295-line full product guide
- `dist/minifw-usb-hospital-standalone-v2.2.0/README.txt` — stub pointing to GUIDE.md
- `dist/minifw-usb-hospital-standalone-v2.2.0/INSTALL.md` — stub pointing to GUIDE.md
- `build_deb.sh` — root-level wrapper delegating to `scripts/build_deb.sh`

**GUIDE.md sections:** Overview, Prerequisites, Quick Start, Dashboard Walkthrough,
Threat Scenarios (ransomware C2, suspicious API leak, DNS tunneling), Configuration,
Admin Reference, Troubleshooting, Production Deployment.

**Key facts:** HTTP port 8000, `admin / Hospital1!`, `bash run_demo.sh`, Python
standalone (no Docker), per-segment thresholds (default block=85, mednet block=45).

**Accuracy fixes (3 rounds of code review):**
- Terminal output prefix corrected: `[minifw-demo]` → `[minifw]`
- Config path corrected: `config/modes/minifw_hospital/` → flat `config/`
- Decision thresholds corrected from flat table to per-segment structure
- Log filename corrected: `minifw_engine.log` → `engine.log`
- policy.json excerpt corrected to match actual `segments`/`features` key structure

**build_deb.sh:** 3-line wrapper using `exec "$(dirname "$0")/scripts/build_deb.sh" "$@"`.
Validated: invalid sector rejected, valid sector prints correct build header.

---

## 2. MINIFW-AI Financial — Live Recovery + Tutorial

**Problem:** All LIVE demo files in `dist/minifw-usb-financial-standalone-v2.2.0/`
were deleted by commit `c2eeba12` ("Cleanup: Menghapus file yang seharusnya diabaikan
oleh .gitignore") because `dist/` is in `.gitignore`. The LIVE deliverable was
effectively broken.

**Recovery:** All 33 files recovered via `git show c2eeba12^:<path>` and restored
in a single commit. Files recovered included:
- `run_demo.sh`, `fast_reset.sh`, `setup_tls.sh`, `teardown_demo.sh`
- `config/policy.json`, `config/feeds/` (14 feed files)
- `demo_data/normal_traffic.json`, `demo_data/attack_traffic.json`
- `models/mlp_model.pkl` (96KB), `scheduler/__init__.py`
- `yara_rules/sme_rules.yar`, `yara_rules/test_rules.yar`
- Supporting docs: `INSTALL.md`, `README.md`, `PRESENTER_CARD.md`, etc.

**Files created:**
- `dist/minifw-usb-financial-standalone-v2.2.0/GUIDE.md` — 443-line full product guide
- `dist/minifw-usb-financial-standalone-v2.2.0/README.txt` — stub pointing to GUIDE.md

**Key facts:** HTTPS port 8443, `admin / Finance1!`, `setup_tls.sh` required before
first run, ArborCrest Capital scenario, trading (10.50.0.0/24) + ERP (192.168.1.0/24)
subnets, 4-step attack chain (Tor recon → C2 beacon → exfil escalation → hard block).

---

## 3. MINIFW-AI Schools — Tutorial

**Files created:**
- `dist/minifw-usb-education-v2.2.0/GUIDE.md` — 282-line full product guide
- `dist/minifw-usb-education-v2.2.0/README.txt` — stub pointing to GUIDE.md

**Key facts:** HTTPS port 8447, `admin / Education1!`, Docker-based, `bash demo.sh`.
Segments: student (MONITOR=35, BLOCK=70), guest (MONITOR=30, BLOCK=60),
staff (MONITOR=50, BLOCK=80), admin (MONITOR=55, BLOCK=85).

**Injector scenarios (8 per loop + burst, ~27s cycle):**
- Westlaw, LexisNexis, courts.gov → ALLOW
- instagram.com → MONITOR (score≈40, student net)
- nordvpn.com → MONITOR; nordvpn-bypass.proxy.io → BLOCK (score=75, YARA VPN match)
- filter-bypass.student.io → BLOCK (score=75, guest net, threshold=60)
- bet365.com → MONITOR (score≈40, student net)
- 200× nordvpn-bypass burst → BLOCK cascade

**Accuracy fixes (code quality review):**
- Event count corrected: "6 scenario events" → "8 scenario events"
- Loop timing corrected: "~33 seconds" → "~27 seconds" (verified from inject.py sleep totals)

---

## 4. MINIFW-AI Legal — Tutorial

**Files created:**
- `dist/minifw-usb-legal-v2.2.0/GUIDE.md` — 285-line full product guide
- `dist/minifw-usb-legal-v2.2.0/README.txt` — stub pointing to GUIDE.md

**Key facts:** HTTPS port 8448, `admin / Legal1!`, Docker-based, `bash demo.sh`.
Segments: partner (MONITOR=55, BLOCK=85), associate (MONITOR=45, BLOCK=72),
paralegal (MONITOR=38, BLOCK=70), client (MONITOR=30, BLOCK=62), guest (MONITOR=28, BLOCK=60).

**Injector scenarios (7 per loop + burst, ~27s cycle):**
- westlaw.com, lexisnexis.com, courts.gov → ALLOW
- wetransfer-legal.io → MONITOR (paralegal, score=40)
- tor-exit-relay.onion-gw.net → BLOCK (client room, score=75)
- clio-encrypt.c2-server.ru → BLOCK (associate, ransomware C2, score=75)
- opposing-counsel.harvest.io → BLOCK (paralegal, privilege breach, score=75)
- 200× clio-encrypt burst → BLOCK cascade

---

## 5. RITAPI V-Sentinel — Tutorial + Installation Program

**Repo:** `/home/sydeco/ritapi-v-sentinel`

**Files created:**
- `dist/usb-vsentinel/GUIDE.md` — 293-line full product guide
- `dist/usb-vsentinel/README.txt` — stub replacing old quick-reference content
- `build_deb.sh` — packages Django + MiniFW services into a .deb

**Key facts:** HTTP ports 8000 (Django Admin) + 8080 (MiniFW Web), `admin / admin123`,
Docker stack (django + minifw + postgres + redis), data seeded automatically on startup.

**build_deb.sh:** Bundles `projects/` + `scripts/` + `install.sh` into
`/opt/ritapi-vsentinel/`, uses existing `packaging/` structure via postinst calling
`install.sh install`. Produces ~102MB `.deb`.

---

## 6. RITAPI Advanced — Tutorial + Installation Program

**Repo:** `/home/sydeco/ritapi/ritapi-adv sc/ritapi-advanced`

**Files created:**
- `dist/ritapi-usb-v1.4.0/GUIDE.md` — full product guide (9 sections)
- `dist/ritapi-usb-v1.4.0/README.txt` — stub pointing to GUIDE.md
- `build_deb.sh` — uses existing `packaging/` directory (DEBIAN/control with
  VERSION_PLACEHOLDER, systemd units, postinst already present)

**Key facts:** HTTP port 8001, no credentials required for dashboard/metrics,
Admin Secret `demo-admin-ritapi-2024` for token issuance. Attack suite runs
automatically on `bash demo.sh` — 5 acts: baseline (200), tenant isolation (403×2),
injection WAF (403×5: SQLi, XSS, CMDi, path traversal, POST SQLi), bot detection
(sqlmap + Nikto → 403), rate limiting (429 at 51st request).

**build_deb.sh:** Overlays `packaging/` template, fills `VERSION_PLACEHOLDER` and
`SIZE_PLACEHOLDER`, copies `app/`, `configs/`, `rules/`, `scripts/`, `requirements.txt`.
Produces 220KB `.deb` (source-only; venv built at install time by postinst).

---

## 7. USB Deployment

All 6 packages copied to `/media/sydeco/3F7C-AB9C` (29GB VFAT USB drive).

| Package | USB Path | Size |
|---------|----------|------|
| Hospital Standalone | `minifw-ai/minifw-usb-hospital-standalone-v2.2.0/` | 247 MB |
| Financial Standalone | `minifw-ai/minifw-usb-financial-standalone-v2.2.0/` | 6.3 MB |
| Schools Docker | `minifw-ai/minifw-usb-education-v2.2.0/` | 912 MB |
| Legal Docker | `minifw-ai/minifw-usb-legal-v2.2.0/` | 911 MB |
| RITAPI Advanced | `ritapi-advanced/ritapi-usb-v1.4.0/` | 257 MB |
| RITAPI V-Sentinel | `ritapi-v-sentinel/usb-vsentinel/` | 1.8 GB |

**Note:** Legal images were stored as a symlink in the repo (`images/minifw-legal.tar
→ build/v2.2.0/demo-legal/images/minifw-legal.tar`). VFAT does not support symlinks
so rsync skipped it. Resolved by copying the real file directly.

USB total: 4.0 GB used, 25 GB remaining.

---

## 8. Git — Pushes

| Repo | Remote | Status |
|------|--------|--------|
| `vadhh/minifw-ai` | `git@github.com:vadhh/minifw-ai.git` | ✅ pushed (rebased over 3 remote commits) |
| `vadhh/ritapi-advanced` | `git@github.com:vadhh/ritapi-advanced.git` | ✅ pushed |
| `vadhh/ritapi-v-sentinel` | `git@github.com:vadhh/ritapi-v-sentinel.git` | ✅ pushed (switched from HTTPS to SSH) |

**minifw-ai rebase note:** Remote had 3 commits (`docs/report-2026-05-26.md`,
hospital-standalone fresh-machine validation files, static-demos spec) that diverged
from local. Resolved with `git pull --rebase` after stashing unstaged changes to
`run_demo.sh` and `scripts/build_deb.sh`.

---

## 9. Test Suite

```
PYTHONPATH=. pytest testing/ -m "not integration" -q
504 passed, 14 warnings in 3.45s
```

0 regressions introduced.
